# Arquivo vazio de proposito: a mera presenca dele aqui faz o pytest inserir
# este diretorio (backend/) no sys.path, permitindo `import app...` tanto com
# `python -m pytest` (que ja faz isso sozinho) quanto com `pytest` direto.
